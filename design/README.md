# Design source

These are the original design comps for the Perch console (static HTML +
screenshots + the design system in `perch_command_interface/DESIGN.md`). The
live console at `perch/web/index.html` is built from this design system and wired
to the API in `perch/api.py`.
